__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/
.env

# TypeScript half. package-lock.json IS committed -- CI runs `npm ci`, and the
# dev toolchain is pinned for the same reason consumers pin a commit.
node_modules/
tsconfig.tsbuildinfo
